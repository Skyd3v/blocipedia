<h1>New Wiki</h1>

 <div class="row">
   <div class="col-md-3">
     <p>Guidelines for Wikis</p>
     <ul>
       <li>Make sure it  has a logic.</li>
       <li>Be aware of sensitive material.</li>
       <li>Post only constructive things.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @wiki do |f| %>
       <div class="form-group">
         <%= f.label :title %>

         <%= f.text_field :title, class: 'form-control', placeholder: "Enter Wiki title" %>
       </div>
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter Wiki body" %>
       </div>
       <div class="form-group">

         <%= f.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>
